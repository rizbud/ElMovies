import {connect} from '@Themes/OsmiProvider';

export default connect({
  container: 'px-3',
  row: 'mx-4 row justify-between items-center mb-2',
  title: 'text-white font-medium text-lg',
  showAll: 'text-red font-regular text-xs opacity-75 text-right',
});
